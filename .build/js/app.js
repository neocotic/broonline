'use strict';

requirejs.config({
    paths: {}
});

require(['jquery', 'bootstrap'], function() {

    var app = {
        initialize: function() {
            // TODO: Something
        }
    };

    app.initialize();

});
